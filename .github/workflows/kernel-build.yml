on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: hyper-2
          fetch-depth: 1
      - name: Hello
        run: echo "hi from kernel build"
